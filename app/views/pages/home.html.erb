<div class="home-callout text-center container-fluid">
  <h1>Bienvenue sur Adopteundev.com</h1>
  <p>Un site de mise en relation entre développeurs web et entrepreneurs</p>
</div>
<div class="container acceuil-souscription">
  <div class="row">
     <% if user_signed_in? %>
       <div class="col-md-6">
         <div class="well">
           <h2 class="text-center">Votre profil</h2>
           <% if current_user.profile %>
             <%= link_to "Edit your profile", edit_user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-lg btn-block' %>
             <%= link_to "View your profile", user_path(id: current_user.id), class: 'btn btn-default btn-lg btn-block' %>
             <% else %>
               <p>Create your profile so that you can share yout information with the community.</p>
               <%= link_to "Create your profile",  new_user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-lg  btn-block' %>
             <% end %>
         </div>
       </div>
       <div class="col-md-6">
         <div class="well">
           <h2 class="text-center">The Community</h2>
           <p>Visit the community to meet other developers, entrepreneurs, and investors!</p>
           <%= link_to "Visit the community", user_path , class: 'btn btn-default btn-lg btn-block' %>
         </div>
       </div>
     <% else %>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Inscription gratuite</h2>
          <h4>Inscrivez-vous gratuitement pour voir les offres proposées.</h4>
          <br/>
          <%= link_to "Souscrire à l'offre de base", new_user_registration_path(plan: @basic_plan.id), class:"btn btn-default btn-lg btn-block" %>
        </div>
      </div>
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Premium</h2>
          <h4>Inscrivez-vous à l'offre premium 10euros/mois sans engagement pour contacter l'annonceur.</h4>
          <%= link_to "Souscrire à l'offre complète", new_user_registration_path(plan: @pro_plan.id), class:"btn btn-primary btn-lg btn-block" %>
        </div>
      </div>
    <% end %>
  </div>
</div>